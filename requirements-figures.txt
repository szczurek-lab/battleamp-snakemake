# Dependencies for regenerating the paper's figures from the result tables
# tracked in this repository. Model submodules, conda environments and a GPU are
# not required.
#
#   pip install -r requirements-figures.txt
#   bash scripts/reproduce_figures.sh
#
# The figures are plotted from tabular data and do not depend on a specific
# release, so these are lower bounds rather than pinned versions.
numpy>=1.21
pandas>=1.3
matplotlib>=3.5
scipy>=1.7
seaborn>=0.11
scikit-learn>=1.0

# Supplies the peptide descriptors used by the synthetic-sequence supplementary
# figure. No other figure imports it.
modlamp>=4.3

# Optional. Separates overlapping point labels in Figure 3, which otherwise
# renders with the labels at their original positions.
adjustText>=0.8
